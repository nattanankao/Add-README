## ทำไม Java สืบทอดหลาย class ไม่ได้
-เพราะจะเกิดปัญหา method ซ้ำกัน (Diamond Problem) ทำให้ไม่รู้จะใช้ของ class ไหน

## ใช้ interface + composition แทนยังไง
-ใช้ implements ได้หลาย interface
-ใช้ composition คือ มี object อยู่ข้างใน (has-a)

## default method ชื่อชนกันแก้ยังไง
ต้อง override เอง แล้วเลือกว่าจะใช้ของ interface ไหน

## ผู้เขียน
นายณัฏฐนัน นามสกุล เอ่งฉ้วน
Nattanan Aengchuan
68122250001
